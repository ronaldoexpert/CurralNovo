[INSEMINACAO]
//ANIMALF = Select ID, NOME, IDENTIFICACAO, PROPRIETARIO, ESTOQUE from ANIMAL where SEXO = 'F' AND TIPO = 'Animal' AND ID NOT IN (SELECT ID_ANIMAL FROM MOVI_INSEMINACAO WHERE CONFIRMADA IN ('A', 'S'))
ANIMALF = Select A.ID, A.NOME, A.IDENTIFICACAO, A.PROPRIETARIO, A.ESTOQUE, COUNT(MV.ID_ANIMAL) AS QTD_INSEMINACAO from ANIMAL A LEFT JOIN MOVI_INSEMINACAO MV ON (A.ID = MV.ID_ANIMAL) where A.SEXO = 'F' AND A.TIPO = 'Animal' AND (MV.CONFIRMADA = 'N' OR MV.CONFIRMADA IS NULL)

PRODUTOR = Select ID, NOME, INSC_RURAL from PRODUTOR

CANCELAR = select 0 AS APAGAR, MI.ID IdMovi, I.ID as IdInsem, I.numero, I.DATA, A.NOME AS ANIMAL, MI.CONFIRMADA, MI.DATA_CONFIRMACAO from MOVI_INSEMINACAO MI JOIN INSEMINACAO I ON (I.ID = MI.ID_INSEMINACAO) JOIN ANIMAL A ON (A.ID = MI.ID_ANIMAL) where MI.CONFIRMADA IN ('S','N') AND I.ID_PRODUTOR = :ID_PRODUTOR ORDER BY I.ID, I.DATA

[MOVIESTOQUE]
PRODUTO  	= select MP.ID, MP.NUMERO, MP.DATA, MP.ID_ANIMAL, MP.ID_PRODUTO, P.DESCRICAO, MP.QUANTIDADE, MP.TIPO, MP.UNIDADE FROM PRODUTO P JOIN MOVI_PRODUTO MP ON (P.ID = MP.ID_PRODUTO) WHERE MP.ID IS NULL
ANIMAL   	= Select ID, NOME, IDENTIFICACAO, ESTOQUE, TIPO from ANIMAL where SEXO = 'M' ORDER BY NOME
PRODUTO_S  	= Select ID, DESCRICAO, UNIDADE, VALOR, ESTOQUE from PRODUTO WHERE SEMEM = 'S' ORDER BY DESCRICAO