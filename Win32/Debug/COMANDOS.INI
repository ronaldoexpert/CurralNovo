[INSEMINACAO]
CANCELAR = select MI.ID as idMovi, I.ID as idInsem, I.numero, I.ID_PRODUTOR, P.NOME AS PROPRIETARIO, I.DATA, A.NOME AS ANIMAL, I.ID_VETERINARIO, V.NOME AS VETERINARIO, MI.CONFIRMADA, MI.DATA_CONFIRMACAO from MOVI_INSEMINACAO MI JOIN INSEMINACAO I ON (I.ID = MI.ID_INSEMINACAO) JOIN ANIMAL A ON (A.ID = MI.ID_ANIMAL) JOIN PRODUTOR P ON (P.ID = I.ID_PRODUTOR) JOIN VETERINARIO V ON (V.ID = I.ID_VETERINARIO) where I.FINALIZADA = :STATUS AND I.ID_PRODUTOR = :ID_PRODUTOR ORDER BY I.ID, I.DATA