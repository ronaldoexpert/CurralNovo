[INSEMINACAO]
PRODUTOR = Select ID, NOME, INSC_RURAL from PRODUTOR
CANCELAR = select MI.ID IdMovi, I.ID as IdInsem, I.numero, I.DATA, A.NOME AS ANIMAL, MI.CONFIRMADA, MI.DATA_CONFIRMACAO from MOVI_INSEMINACAO MI JOIN INSEMINACAO I ON (I.ID = MI.ID_INSEMINACAO) JOIN ANIMAL A ON (A.ID = MI.ID_ANIMAL) where MI.CONFIRMADA IN ('S','N') AND I.ID_PRODUTOR = :ID_PRODUTOR ORDER BY I.ID, I.DATA